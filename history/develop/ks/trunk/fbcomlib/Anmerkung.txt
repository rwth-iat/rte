Anmerkungen zu fbcomlib:

- Als Servername könnte "localhost" bei manchen Windows Rechnern Problem führen. Es sollte "127.0.0.1" verwendet werden.
  Port wie z.B. 7509 muss nicht eingegeben werden.
  
- channel object MUSS "channel" heißen; Lars: it's a feature, war von chef so gewollt 


Liyong

Benutzung:

- fbcomlib objekte anlegen: die zugrhörigen ksapi-objekte werden selbstständig angelegt
- zu jedem ksapi-objekt muss ein channel angelegt werden, momentan gibt es nur den tcp-channel aus der ksapi-tcp. das channel-objekt muss channel heißen
- doSend löst den sende-/hol-Vorgang aus
- doCyclic bewirkt abholen/senden im takt des fbcomlib-Objekts, sofern iexreq == 1
	- ist iexreq == 0, aber doCyclic == 1 werden Set*-Blöcke durch eine Änderung des zu sendenden Werts getriggert